// Напишите функцию min(a,b) , которая возвращает меньшее из чисел a,b .
function min(a,b) {
    let minVal = a;
    return (minVal < b) ? minVal : b;
}

console.log(min(1, 1));
// min(3, ‐1);
// min(1, 1);